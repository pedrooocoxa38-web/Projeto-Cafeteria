{
  "name": "geekhaven-brew",
  "build": {
    "commands": [
      "npm install",
      "npm run build"
    ]
  },
  "deploy": {
    "restartPolicyType": "ON_FAILURE",
    "sleepApplication": false,
    "staticPublishPath": "dist"
  }
}